# Python
__pycache__/
*.py[cod]
.pytest_cache/

# Virtual environments
.venv/
venv/
env/

# Env files with secrets (keep the .env.example templates)
.env
backend/.env

# Local SQLite DB (regenerate via `python ingest.py`)
backend/laptops.db
*.db

# Phase 5 live-price JSON cache (regenerates on demand, 24h TTL)
backend/.cache/

# OS
.DS_Store
Thumbs.db

# Claude Code local tooling state
.claude/
